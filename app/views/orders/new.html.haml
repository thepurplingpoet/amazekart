%h2 Checkout!!

= form_tag orders_path pamethod: :post do |form|
  - @orderitems.each do |item|
    %p
      = label_tag Product.find(item).name
      = number_field_tag "cart[#{item.to_s}]", '1'
  %p
    = submit_tag "Checkout" 

    
